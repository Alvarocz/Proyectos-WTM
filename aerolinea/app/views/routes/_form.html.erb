<%= form_for(@route) do |f| %>
  <% if @route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@route.errors.count, "error") %> prohibited this route from being saved:</h2>

      <ul>
      <% @route.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Origen" %><br>
    <%= f.text_field :origin %>
  </div>
  <div class="field">
    <%= f.label "Destino" %><br>
    <%= f.text_field :destination %>
  </div>
  <div class="field">
    <%= f.label "Aerolinea" %><br>
    <%= f.select :airline_id, Airline.all.collect { |a| [a.name, a.id] } %>
  </div>
  </div>
  <div class="field">
    <%= f.label "Pilotos" %><br>
    <%= f.collection_check_boxes :pilot_ids, Pilot.all, :id, :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
